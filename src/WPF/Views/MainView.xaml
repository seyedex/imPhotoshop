<UserControl
    x:Class="imPhotoshop.WPF.Views.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:imPhotoshop.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="5*"  />
            <ColumnDefinition Width="*" MinWidth="250" />
        </Grid.ColumnDefinitions>

        <!--  LEFT PANEL  -->
        <ContentControl x:Name="ToolPanel" Grid.Column="0" />

        <!--  CANVAS  -->
        <ContentControl x:Name="Canvas" Grid.Column="1" />

        <!--  RIGHT PANEL  -->
        <ContentControl x:Name="Workspace" Grid.Column="2" />
    </Grid>
</UserControl>
